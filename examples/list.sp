// Main method called when the script starts
method main() {
    // Create new list
    List list = new List();
    // Add a few things
    list.add("1");
    list.add("2");
    // Print the list
    System.print(list);
}