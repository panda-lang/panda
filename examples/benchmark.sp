
method main() {

    for (10000) {
        System.print("Hello Panda");

        if (true) {
            String var = "Variable Test";
            System.print(var);

            var = null;
            System.print(var);
        }

        for (1) {
            pandaInfo();
        }

        Int x = 0;
        while (x < 1) {
            System.print(5 < 10);
            x = 1;
        }

    }

}

method pandaInfo() {
    System.print("Hello Panda");
    System.print(Panda.getVersion());
}