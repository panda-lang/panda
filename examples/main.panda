module example.test;

import panda.lang;
import java.lang;

main {
    App app = new App();
    app.start();

    Console.print(App.getVersion());
}

class App {

    static String version = "indev-0.5.0";

    constructor () {
        Console.print(getVersion());
    }

    method start() {
        // [...]
    }

    static String getVersion() {
        return version;
    }

}